version: 0.1

phases:
  build:
    commands:
      - npm install
      - npm run Webpack

artifacts:
  files:
    - index.html
    - favicon.ico
    - AWS*.pdf
    - styles/*
    - images/*
    - dist/bundle.js
